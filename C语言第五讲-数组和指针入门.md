C语言第五讲-数组和指针入门

* &#x20; 1.数组
* 数组的声明和使用
* 数组的格式：数组名+【数字下标】

```c
int main()
int num[3]={0};
{
pirntf("输入学号");
  sacnf("%d",&num);
printf（"学号是%d",num[3];
}
```

这表示数组包含3个整型从零开始计数

​

* 多维数组

以二维数组为例

num\[5]\[9]  表示5列9行的数组

* 数组和内存的结构

计算机抽象层：只有一行，按顺序排列的01字节，将数组按顺序变成二进制后每个数组代表一个字节,是连续的一段数字

C语言逻辑抽象层

按照二维数组和数组排序

1. 超过排序，将会在原来基础上+1.
2. 常量数组

const关键词，在代码运行中不会被修改的值，节约内存

const char a\[]=\[‘0’，’1‘，’2‘，’3‘]；

表示四个常量类型的字符

​

* 指针

每个指针都指向唯一的字节

a.指针变量

用来存储地址的变量，它的值是某个数据的地址，可以指向普通变量、数组、字符串、函数，甚至是另一个指针变量。通过指针变量，可以间接访问或修改内存中的数据。

格式为：数据类型 \*指针变量名

如：

```c
int i = 5, j = 10;
int *p = &i, *q = &j;
q = p; // 将指针p的值赋给q，此时q也指向变量i
```

​

指针会对变化的字符进行改变如：

```c
#include <stdio.h>
int main()
{
int a=1;
int*p=NULL;
p=&a;
*p=5;
printf("%d",a);
return 0;
}
```

​
