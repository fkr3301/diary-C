# C语言知识一

**<span style="background-color: #FFFFFF;">所有中文字符改为英文字符</span>**

* **1.转义序列**
  1. \\+字母组成转义序列
  2. \n是换行
  3. \r是回到开头
* **2.变量与赋值**

整数int 不能以数字开头，不能复用

小数float

1. float和double区别

float为44字节（32位）

double为8个字节（64位）

float累积误差快

double精读较高

* &#x20;**3.赋值运算符**
  1. &#x20;左边变量是被赋予，即左边必须是变量，左边不能是表达式
  2. %d为格式说明符，存入的变量由输出和表达变量来表示如printf("%d",num);会把“num”打印出来。
  3. scanf("%d,\&a")告诉读取键盘输入整数存入变量。
* &#x20;**4.预处理**

编译之前先处理的

1. \#include 包含
2. \#define 宏定义
3. n表示最小栏宽，如果不足将会左侧补空格，超过则正常显示
4. %n.md      m表示有效位数，不足补0
5. %n.mf       表示小数点后有效位数，默认后6位

​
